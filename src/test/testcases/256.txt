
        var a, b, c: real;
        var x, y, z: Boolean;
        

        function n(): Real;
        var x, y, z: Integer;
        begin
            readLn();
            fs := readStdin();
            
            with i: integer; do begin
                for i := 6 downto -1 do h := 6 + i;
                if i > 6 then return 0;
            end
            return 1;
        end
        var w : integer;

        function bj(): Boolean;
        var a: string;
        begin 
            (*       
        end
        